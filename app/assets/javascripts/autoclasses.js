$(document).ready(function () {

    // Add bootstrap datepicker
    $('#autoclass_start_date').datepicker({
        format: 'yyyy-mm-dd',
        autoclose: true
    })

    // Add bootstrap datepicker
    $('#autoclass_stop_date').datepicker({
        format: 'yyyy-mm-dd',
        autoclose: true
    })

});
